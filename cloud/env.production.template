# Production Environment Variables for Doc Time Backend
# Copy these to your Cloud Run service environment variables
# Or use: gcloud run services update doctime-backend --update-env-vars-file=env.production

# Cloud SQL Connection
# Format when using Cloud SQL proxy: postgresql://USER:PASSWORD@/DATABASE?host=/cloudsql/CONNECTION_NAME
DATABASE_URL=postgresql://doctime_user:DoctimeCloud2024Secure@/doctime?host=/cloudsql/drink-suite:us-central1:doctime-db

# Database Configuration
DB_REQUIRE_SSL=true
DB_POOL_MAX=3
DB_POOL_MIN=1

# JWT Configuration
JWT_SECRET=RNfWzy5JvlwIHgKg2KtZMbYmtwG4iHY6GMa5J6AcCDA=

# Server Configuration
NODE_ENV=production
PORT=8080

# SMS Configuration (Advanta)
ADVANTA_API_KEY=your-advanta-api-key
ADVANTA_PARTNER_ID=your-partner-id
ADVANTA_SENDER_ID=your-sender-id
ENABLE_SMS=true

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
EMAIL_FROM=your-email@gmail.com

# Rate Limiting
RATE_LIMIT_MAX=100

# CORS (for web app - update with your actual domain)
CORS_ORIGIN=https://your-web-app-domain.com

